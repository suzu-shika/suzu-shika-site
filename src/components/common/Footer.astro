<!-- footer -->
<footer class="footer">
  <div class="l-container">
    <div class="footer-content">
      <div class="footer-logo">
        <a href="/">
          <img src="/assets/images/global/logo-white.svg" alt="鈴鹿Web制作" loading="lazy" decoding="async" width="238" height="32">
        </a>
        <p class="footer-logo-text">埼玉県所沢市を拠点に、<br>ホームページ制作を行っています</p>
      </div>
      <nav class="footer-nav" aria-label="フッターナビゲーション">
        <ul class="footer-nav-list">
          <li class="footer-nav-item">
            <a href="/">ホーム</a>
          </li>
          <li class="footer-nav-item">
            <a href="/contact">お問い合わせ</a>
          </li>
          <li class="footer-nav-item">
            <a href="/privacy-policy">プライバシーポリシー</a>
          </li>
        </ul>
      </nav>
      <div class="footer-links">
        <ul class="footer-sns-list">
          <li class="footer-sns-item">
            <a href="https://x.com/suzu_shika_web" target="_blank" rel="noopener noreferrer">
              <img src="/assets/images/global/icon-x.svg" alt="X (Twitter)" loading="lazy" decoding="async" width="24" height="24">
            </a>
          </li>
          <li class="footer-sns-item">
            <a href="https://www.instagram.com/suzu_shika_web/" target="_blank" rel="noopener noreferrer">
              <img src="/assets/images/global/icon-instagram.svg" alt="Instagram" loading="lazy" decoding="async" width="24" height="24">
            </a>
          </li>
        </ul>
        <div class="footer-blog">
          <a href="https://blog.suzu-shika.com/" target="_blank" rel="noopener noreferrer">技術ブログ<span class="u-visually-hidden">（新しいタブで開きます）</span></a>
        </div>
      </div>
    </div>
    <div class="footer-copyright">
      <small aria-label="著作権 鈴鹿Web制作">
        &copy; Suzu-Shika Web Production
      </small>
    </div>
  </div><!-- /.l-container -->

  <!-- トップへ戻るボタン -->
  <a href="#body" class="footer-page-top">
    <span class="u-visually-hidden">ページトップへ戻る</span>
  </a>
</footer>

<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const pageTop = document.querySelector(".footer-page-top");
    const footer = document.querySelector(".footer");

    if (!pageTop || !footer) return;

    // 初期状態
    pageTop.style.opacity = "0";
    pageTop.style.pointerEvents = "none";

    let footerVisible = false;

    // フッター検知
    // const observer = new IntersectionObserver(
    //   (entries) => {
    //     footerVisible = entries[0].isIntersecting;
    //     toggleButton();
    //   },
    //   {
    //     root: null,
    //     threshold: 0
    //   }
    // );

    // observer.observe(footer);

    // スクロール監視
    window.addEventListener("scroll", toggleButton);

    // ページトップへ戻る
    pageTop.addEventListener("click", (e) => {
      e.preventDefault();
      window.scrollTo({
        top: 0,
        behavior: "smooth",
      });
    });

    function toggleButton() {
      const scrollY = window.scrollY;

      if (scrollY > 200 && !footerVisible) {
        pageTop.style.opacity = "1";
        pageTop.style.pointerEvents = "auto";
      } else {
        pageTop.style.opacity = "0";
        pageTop.style.pointerEvents = "none";
      }
    }
  });
</script>

<style lang="scss">
  @use "../../styles/breakpoints" as *;

  .footer {
    padding-block: 80px 24px;
    background-color: var(--color-main);
    color: var(--color-white);
  }

  .footer-content {
    display: grid;
    justify-items: center;
    gap: 56px;
  }

  .footer-logo {
    display: grid;
    justify-items: center;
    gap: 32px;

    img {
      max-width: 179px;
    }
  }

  .footer-logo-text {
    font-size: 14px;
    text-align: center;
  }

  .footer-nav-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 8px 24px;
    padding: 0;
  }

  .footer-nav-item {
    a {
      display: inline-block;
      padding-block: 8px;
      font-size: 14px;
      font-weight: 700;
      line-height: 1;
    }
  }

  .footer-links {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
  }

  .footer-sns-list {
    display: flex;
    gap: 24px;
    padding: 0;
  }

  .footer-sns-item {
    img {
      width: 28px;
    }
  }

  .footer-blog {
    a {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding-block: 8px;
      font-size: 14px;
      line-height: 1;

      &::after {
        content: "";
        width: 15px;
        height: 13px;
        background-image: url(/assets/images/global/icon-external-link-white.svg);
        background-repeat: no-repeat;
        background-size: contain;
        background-position: center center;
      }
    }
  }

  .footer-copyright {
    margin-block-start: 24px;
    text-align: center;

    small {
      display: inline-block;
      font-size: 12px;
    }
  }

  @include mq() {
    .footer-content {
      grid-template-columns: auto auto;
      grid-template-rows: auto auto;
      justify-items: self-start;
      gap: 80px 40px;
    }

    .footer-logo {
      justify-items: self-start;
      gap: 12px;

      img {
        max-width: 238px;
      }
    }

    .footer-logo-text {
      font-size: 12px;
      text-align: left;

      br {
        display: none;
      }
    }

    .footer-nav {
      grid-column: 1 / 2;
    }

    .footer-nav-list {
      gap: clamp(24px, calc(32 / 1024 * 100vw) ,32px);
    }

    .footer-nav-item {
      a {
        font-size: 16px;
        font-weight: 400;
      }
    }

    .footer-links {
      grid-column: 2 / 3;
      grid-row: 2 / 3;
      justify-self: end;
      flex-direction: row;
      gap: 24px;
    }

    .footer-sns-item {
      img {
        width: 24px;
      }
    }

    .footer-copyright {
      margin-block-start: 120px;
    }
  }

  @media (any-hover: hover) {
    .footer-nav-item a,
    .footer-blog a {
      position: relative;

      &::before {
        content: "";
        width: 0;
        height: 1px;
        background-color: currentColor;
        position: absolute;
        bottom: 2px;
        left: 0;
        transition: width .3s ease;
      }

      &:hover::before {
        width: 100%;
      }
    }

    .footer-sns-item a {
      transition: filter .3s ease;

      &:hover {
        filter: brightness(80%);
      }
    }
  }

  // トップへ戻るボタン
  .footer-page-top {
    display: grid;
    place-items: center;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 2px solid;
    color: #2c696f;
    opacity: 0;
    pointer-events: none;
    transition: opacity .3s;
    position: fixed;
    right: 20px;
    bottom: 10px;
    transition: background-color .3s ease;

    &::after {
      content: "";
      width: 14px;
      height: 14px;
      border-top: 3px solid currentColor;
      border-left: 3px solid currentColor;
      margin-block-start: 8px;
      rotate: 45deg;
      transition: border-color .3s ease;
    }

    @include mq() {
      width: 48px;
      height: 48px;
      right: 24px;
      bottom: 24px;

      &::after {
        width: 16px;
        height: 16px;
      }
    }
  }

  @media (any-hover: hover)  {
    .footer-page-top {
      &:hover {
        background-color: currentColor;

        &::after {
          border-color: var(--color-white);
        }
      }
    }
  }

</style>
